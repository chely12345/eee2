<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cly.eee">
    <uses-permission
        android:name="android.permission.CHANGE_CONFIGURATION"
        tools:ignore="ProtectedPermissions" />

    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
    <activity
        android:configChanges="orientation"
        android:name=".drawerlayout.MainActivityDrawerLayout"
        android:label="@string/app_name" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
        <activity android:name=".viewpager.OneActivity"/>
        <activity android:name=".viewpager.TwoActivity"/>
        <activity android:name=".viewpager.ThreeActivity"/>
        <activity android:name=".viewpager.FourActivity"/>

        <activity android:name=".notification.OtherActivityNotification"/>

        <activity android:name=".drawerlayout.OtherActivityDrawerLayout"/>





</application>

</manifest>
